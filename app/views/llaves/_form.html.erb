<%= form_with(model: llafe, local: true) do |form| %>
  <% if llafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(llafe.errors.count, "error") %> prohibited this llafe from being saved:</h2>

      <ul>
        <% llafe.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :etapa %>
    <%= form.text_field :etapa %>
  </div>

  <div class="field">
    <%= form.label :numero %>
    <%= form.text_field :numero %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
