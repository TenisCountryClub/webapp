<p id="notice" class="alert alert-success"><span id ="alert"><%= notice %></span><button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button></p>

<h1>Torneos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Fecha de inicio</th>
      <th>Fecha de fin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @torneos.each do |torneo| %>
      <tr>
        <td><%= torneo.nombre %></td>
        <td><%= torneo.fecha_inicio %></td>
        <td><%= torneo.fecha_fin %></td>
        <td><%= link_to 'Mostrar', torneo %></td>
        <td><%= link_to 'Editar', edit_torneo_path(torneo) %></td>
        <td><%= link_to 'Destruir', torneo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Torneo', new_torneo_path, class: "btn btn-secondary" %>

<script type="text/javascript">
  if ($('#notice').is(':empty')) {
    $('#notice').css('display','none');
  }
</script>